# Architecture

The product constraints governing provider boundaries are canonical in the
[product pitch](../product/pitch.md). Implementation topology has not been accepted
yet and will be recorded when a capability is promoted to a Spec.
